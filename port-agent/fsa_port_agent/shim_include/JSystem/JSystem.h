#ifndef JSYSTEM_H
#define JSYSTEM_H
/* Stub: bypass TWW's JSystem.mch/.pch monolithic precompiled-header include.
   Matches FSA's own fsa-decomp/include/JSystem/JSystem.h approach — each
   TU must include only what it explicitly needs, giving bytes closer to FSA's
   real compile (which doesn't use precompiled headers either). */
#endif
